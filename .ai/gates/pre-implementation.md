# 实现前门禁

在写正式业务代码**之前**通过（用户确认的一行小修可豁免）。

## 检查清单

- [ ] 范围清晰：涉及哪些 packages、路由/接口
- [ ] 已调研现有代码（grep/读邻近文件）；优先复用
- [ ] 已有计划，或用户明确免计划
- [ ] 涉及 API 变更 → 将阅读 `api-contract` gate
- [ ] 涉及 UI 变更 → 将阅读 `ui-quality` gate
- [ ] 无需把密钥或 env 值写入仓库

## 可豁免的情况

用户说「就改 X 就行」，且单文件改动 < ~20 行、意图明确。
