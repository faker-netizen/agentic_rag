/** Length of the `event:` SSE field prefix */
export const SSE_EVENT_PREFIX_LEN = 6;
/** Length of the `data:` SSE field prefix */
export const SSE_DATA_PREFIX_LEN = 5;

/** 读流空闲超时（毫秒） */
export const SSE_IDLE_TIMEOUT_MS = 90_000;
